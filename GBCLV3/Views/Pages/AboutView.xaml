<ContentControl x:Class="GBCLV3.Views.Pages.AboutView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:s="https://github.com/canton7/Stylet"
                xmlns:adonisUI="clr-namespace:AdonisUI;assembly=AdonisUI"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                mc:Ignorable="d" 
                d:DesignHeight="450" d:DesignWidth="800"
                Style="{StaticResource BlurInPageStyle}">

    <Grid Background="#80FFFFFF">

        <Path x:Name="BossWhatAreYouDoingBoss"
              HorizontalAlignment="Right" VerticalAlignment="Bottom"
              Stretch="Uniform" Margin="32,48,32,16"
              Data="{StaticResource DontYouEverStop}" Opacity="0.6"
              Fill="{DynamicResource {x:Static adonisUI:Brushes.ForegroundBrush}}"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="196"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="1" Margin="32,0"
                       HorizontalAlignment="Right" VerticalAlignment="Center"
                       FontSize="24" Opacity="0.3"
                       FontStyle="Italic"
                       Text="{DynamicResource DontStop}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="32,0"
                       HorizontalAlignment="Left" VerticalAlignment="Center"
                       FontSize="32"
                       Text="{Binding GBCLV3}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Margin="32,0"
                       HorizontalAlignment="Left" VerticalAlignment="Center"
                       FontSize="16"
                       Text="{Binding VersionCode}"/>

        </Grid>
    </Grid>


</ContentControl>
